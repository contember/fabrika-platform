# @fabrika/runner-contract

Provider-neutral Worker↔container transport types and endpoint constants. This package has no runtime
implementation and does not own provider job semantics.

`RunLogLine` is owned by `@fabrika/control-contract` and re-exported here for transport consumers.
