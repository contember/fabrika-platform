{
	"name": "fabrika-runner-image",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/auth-core",
		"packages/auth",
		"packages/config",
		"packages/engine",
		"packages/platform",
		"packages/runner"
	],
	"//": "Slimmed workspace root baked into the runner image. Local @fabrika packages resolve from the copied workspaces. oblaka-iac is pinned to the version that ships the programmatic deploy() the engine calls; wrangler is installed globally in the image.",
	"dependencies": {
		"oblaka-iac": "^0.0.18"
	}
}
